branches:
  - main
packages:
  - name: hooray
    path: .
    type: rust
    manifest: Cargo.toml
    changelog: CHANGELOG.md
    scopes:
      - hooray
hooks:
  afterVersion:
    - scripts/sync-action-versions.sh
    - cargo generate-lockfile
github:
  releases: true
